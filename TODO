
* pow/pow.js: class based pow files with register and before_cmd
* pow/pow.js: update pow+file an base_dir via a decorator
* pow/utils.js: support for check, shell and timeout
* pow_files/pow_bad.js: move pow cosmo-* to docker
* pow_files/pow_bad.js: run pow todo in Docker
* pow_files/pow_good.js: pow update without docker (using plain zip)

